; Basic
P = Player
X = box spot (without the box is on)
B = Box
E = Empty space (that can be moving within)
W = Wall
e = empty space outside playground
x = box already on X spot when initiated

; Teleport variant
T = Teleport

; Conveyor belt variant
C = Conveyor belt (always right)

; Hole variant
H = Hole

; Duo player variant
p = player 2

; Bot variant
Y = bot

; Switch variant
S = Switch
G = Gate

